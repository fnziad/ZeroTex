# © 2025 Fahad Nadim Ziad — https://github.com/fnziad

# Prevent pushes with --no-verify
if [ -n "$HUSKY_SKIP_HOOKS" ]; then
  echo "⚠️  Skipping pre-push hooks (not recommended for main/develop)"
  exit 0
fi

echo "🚀 Running pre-push checks..."

# Get current branch
BRANCH=$(git rev-parse --abbrev-ref HEAD)

if [ "$BRANCH" = "main" ] || [ "$BRANCH" = "master" ] || [ "$BRANCH" = "develop" ]; then
  echo "🔒 Pushing to protected branch: $BRANCH"
  echo "✓ Verifying all checks..."
  
  npm run lint || exit 1
  
  echo "✅ Pre-push checks passed!"
fi
